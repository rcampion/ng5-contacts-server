DROP TABLE PCM_CONTACT_PHONES;

CREATE TABLE PCM_CONTACT_PHONES (
		ID INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY  (START WITH 1 ,INCREMENT BY 14),
		CONTACT_ID INTEGER NOT NULL,
		PHONE VARCHAR(100),
		PHONEKIND INTEGER DEFAULT 1 NOT NULL,
		PRIMARY KEY (ID)
	)