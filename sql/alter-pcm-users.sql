ALTER TABLE PCM_AUTHORITIES DROP CONSTRAINT FK_4DUYQ3MMTRLDR30ORQ5HFCBXL;
ALTER TABLE CONTACTDTOORIG DROP CONSTRAINT SQL150902094915160;
DROP TABLE CONTACTDTOORIG;

ALTER TABLE PCM_USERS DROP CONSTRAINT SQL120217135903340;

DROP TABLE PCM_USERS;

CREATE TABLE PCM_USERS (
		ID INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY  (START WITH 1 ,INCREMENT BY 4),
		LOGIN VARCHAR(100) NOT NULL,
		USERNAME VARCHAR(100) NOT NULL,
		FIRSTNAME VARCHAR(100) NOT NULL,
		LASTNAME VARCHAR(100) NOT NULL,
		PASSWORD VARCHAR(100) NOT NULL,
		ENABLED INTEGER DEFAULT 1 NOT NULL,
		PUBLIC_SECRET VARCHAR(100),
		PRIVATE_SECRET VARCHAR(100)
	);
	
ALTER TABLE PCM_USERS ADD CONSTRAINT SQL120217135903340 PRIMARY KEY (ID);
	
CREATE UNIQUE INDEX SQL120217135903341 ON PCM_USERS (USERNAME ASC);
		
ALTER TABLE PCM_AUTHORITIES ADD CONSTRAINT FK_4DUYQ3MMTRLDR30ORQ5HFCBXL FOREIGN KEY (USERNAME)
	REFERENCES PCM_USERS (USERNAME);
	